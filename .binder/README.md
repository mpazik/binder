# Binder Workspace

This directory contains the Binder knowledge graph configuration for the project itself — a real-world example of Binder in action.

Only curated resources are included in the repository. The rest is maintained separately as it changes frequently and is mostly internal scratchpad information.
