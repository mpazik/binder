# Project management blueprint
# Provides: Task, Milestone types with Markdown navigation

# ====================
# SCHEMA
# ====================

- author: system
  configs:
    - key: size
      type: Field
      name: Size
      description: Scope and complexity of the task
      dataType: option
      options:
        - key: small
          name: Small
          description: Quick to complete, no decisions needed
        - key: medium
          name: Medium
          description: Some coordination or multiple steps
        - key: large
          name: Large
          description: Significant effort, non-trivial decisions required

    - key: acceptanceCriteria
      type: Field
      name: Acceptance Criteria
      dataType: richtext
      richtextFormat: line
      allowMultiple: true
      description: Conditions that must be met for this task to be considered done

    - key: constraints
      type: Field
      name: Constraints
      dataType: richtext
      richtextFormat: block
      allowMultiple: true
      description: Boundaries that must be respected

    - key: outOfScope
      type: Field
      name: Out of Scope
      dataType: richtext
      richtextFormat: block
      allowMultiple: true
      description: What is deliberately not covered by this task

    - key: Task
      type: Type
      name: Task
      fields:
        - title: { required: true }
        - description
        - details
        - content
        - status
        - priority
        - size
        - dueDate
        - acceptanceCriteria
        - constraints
        - outOfScope
        - parent: { only: [Task] }
        - children: { only: [Task] }
        - partOf: { only: [Milestone] }
        - requires: { only: [Task] }
        - requiredBy: { only: [Task] }
        - relatesTo: { only: [Task] }
        - tags

    - key: Milestone
      type: Type
      name: Milestone
      fields:
        - title: { required: true }
        - description
        - details
        - status
        - dueDate
        - acceptanceCriteria
        - constraints
        - contains: { only: [Task] }
        - requires: { only: [Milestone] }
        - requiredBy: { only: [Milestone] }

# ====================
# NAVIGATION
# ====================

- author: system
  configs:
    # --- Task view ---
    - key: task-view
      type: View
      name: Task
      preamble: [key, title, status, priority, size, dueDate, description, tags, partOf, requires, requiredBy, relatesTo, parent, children]
      viewContent: |
        # {title}

        ## Details

        {details}

        ## Acceptance Criteria

        {acceptanceCriteria}

        ## Constraints

        {constraints}

        ## Out of Scope

        {outOfScope}

        ---

        {content}

    # --- Task navigation ---
    - key: nav-tasks
      type: Navigation
      path: tasks/{title}
      where: { type: Task, status: [pending, active, paused] }
      view: task-view

    - key: nav-archive-tasks
      type: Navigation
      path: archive/tasks/{title}
      where: { type: Task, status: [complete, cancelled] }
      view: task-view

    # Backlog index
    - key: nav-backlog
      type: Navigation
      path: backlog
      query:
        filters:
          type: Task
          partOf: { op: empty, value: true }
          status: [pending, active]

    # --- Milestone views ---
    - key: task-list-item
      type: View
      name: Task List Item
      viewFormat: line
      viewContent: |
        - {title} ({priority})

    - key: milestone-view
      type: View
      name: Milestone
      preamble: [key, title, status, dueDate, description, requires, requiredBy]
      viewContent: |
        # {title}

        ## Details

        {details}

        ## Acceptance Criteria

        {acceptanceCriteria}

        ## Constraints

        {constraints}

        ## Pending

        {contains|where:status=pending|view:task-list-item}

        ## Active

        {contains|where:status=active|view:task-list-item}

        ## Complete

        {contains|where:status=complete|view:task-list-item}

    # --- Milestone navigation ---
    - key: nav-milestones-index
      type: Navigation
      path: milestones
      query:
        filters: { type: Milestone, status: { op: notIn, value: [cancelled] } }
        includes: { key: true, title: true, status: true, dueDate: true }

    - key: nav-milestones
      type: Navigation
      path: milestones/{key}
      where: { type: Milestone }
      view: milestone-view

# ====================
# SAMPLE DATA
# ====================

- author: system
  records:
    # --- Milestones ---
    - type: Milestone
      key: mvp-release
      title: MVP Release
      description: Ship core functionality to beta testers
      status: active
      dueDate: 2025-02-15

    - type: Milestone
      key: public-beta
      title: Public Beta
      description: Open access beta with onboarding flow and docs
      status: pending
      dueDate: 2025-04-01
      requires: [mvp-release]

    - type: Milestone
      key: v1-launch
      title: V1 Public Launch
      description: App store release with stable API
      status: pending
      dueDate: 2025-06-01
      requires: [public-beta]

    # --- Tasks: MVP Release ---
    - type: Task
      key: build-auth
      title: Build authentication flow
      description: Login, signup, password reset
      status: active
      priority: p2
      partOf: mvp-release
      requires: [setup-database]
      content: |
        # Implementation plan

        ## Phase 1: Backend

        Wire up JWT issuance and refresh endpoints. Store hashed passwords with bcrypt.

        ## Phase 2: UI

        Build login, signup, and reset screens. Add form validation and error states.

        ## Open questions

        - Should reset links expire after 1 hour or 24 hours?
        - Do we need email verification at signup?

    - type: Task
      key: setup-database
      title: Set up database schema
      description: Define core tables and run initial migrations
      status: complete
      priority: p2
      partOf: mvp-release

    - type: Task
      key: api-endpoints
      title: Implement REST API endpoints
      description: CRUD endpoints for all core entities
      status: active
      priority: p2
      partOf: mvp-release
      requires: [setup-database]

    - type: Task
      key: fix-landscape-bug
      title: Fix landscape orientation bug
      description: Orientation breaks on tablets when rotating during navigation transitions
      status: active
      priority: p3
      partOf: mvp-release

    # --- Tasks: Public Beta ---
    - type: Task
      key: design-onboarding
      title: Design onboarding screens
      description: First-run experience with account setup and feature tour
      status: pending
      priority: p3
      partOf: public-beta

    - type: Task
      key: write-user-guide
      title: Write user guide
      description: Getting started docs, FAQ, and troubleshooting
      status: pending
      priority: p3
      partOf: public-beta

    - type: Task
      key: invite-system
      title: Build invite system
      description: Generate and validate beta invite codes
      status: pending
      priority: p4
      partOf: public-beta

    # --- Tasks: V1 Launch ---
    - type: Task
      key: app-store-copy
      title: App store listing copy
      description: Screenshots, description, and metadata for app store submission
      status: pending
      priority: p4
      partOf: v1-launch

    - type: Task
      key: performance-audit
      title: Performance audit
      description: Profile and optimize startup time, memory usage, and render performance
      status: pending
      priority: p3
      partOf: v1-launch

    # --- Unassigned tasks (backlog) ---
    - type: Task
      key: dark-mode
      title: Add dark mode support
      description: System-aware theme switching with manual override
      status: pending
      priority: p4

    - type: Task
      key: export-csv
      title: CSV export for lists
      description: Export any list view as a CSV file
      status: active
      priority: p4
