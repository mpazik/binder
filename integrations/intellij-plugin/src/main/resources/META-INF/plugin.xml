<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>do.binder.plugin</id>
    <name>Binder</name>
    <vendor email="support@binder.do" url="https://binder.do">Binder</vendor>

    <description><![CDATA[
    Language support for <a href="https://binder.do">Binder</a> — a local-first knowledge base with bidirectional Markdown sync.

    <h3>Features</h3>
    <ul>
        <li>Autocomplete for link references, field names, and valid option values</li>
        <li>Hover docs with field descriptions and allowed values</li>
        <li>Go to definition for referenced entities</li>
        <li>Inlay hints showing referenced entity titles inline</li>
        <li>Code actions and quick fixes for invalid field values</li>
        <li>Real-time diagnostics against your schema</li>
        <li>Automatic sync on save</li>
    </ul>

    <h3>Requirements</h3>
    <ul>
        <li>Binder CLI installed and available in PATH</li>
        <li>A Binder workspace at project root or in an immediate subdirectory (contains a <code>.binder</code> directory)</li>
    </ul>

    Configure the CLI path at <b>Settings &gt; Tools &gt; Binder</b>.
    ]]></description>

    <change-notes><![CDATA[
    <h3>0.1.0</h3>
    <ul>
        <li>Autocomplete for link references, field names, and valid option values</li>
        <li>Hover docs with field descriptions and allowed values</li>
        <li>Go to definition for referenced entities</li>
        <li>Inlay hints showing referenced entity titles inline</li>
        <li>Code actions and quick fixes for invalid field values</li>
        <li>Real-time diagnostics against your schema</li>
        <li>Automatic sync on save</li>
        <li>Configurable Binder executable path via Settings &gt; Tools &gt; Binder</li>
    </ul>
    ]]></change-notes>

    <!-- Product and plugin compatibility requirements -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lsp</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- LSP Server Support -->
        <platform.lsp.serverSupportProvider
            implementation="com.binder.intellij.lsp.BinderLspServerSupportProvider"/>

        <!-- Settings Service -->
        <applicationService
            serviceImplementation="com.binder.intellij.settings.BinderSettings"/>

        <!-- Settings UI -->
        <applicationConfigurable
            parentId="tools"
            instance="com.binder.intellij.settings.BinderSettingsConfigurable"
            id="do.binder.plugin.settings.BinderSettingsConfigurable"
            displayName="Binder"/>
    </extensions>
</idea-plugin>
